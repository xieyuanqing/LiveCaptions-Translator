<ui:FluentWindow
    x:Class="LiveCaptionsTranslator.WelcomeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LiveCaptionsTranslator"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Getting Started"
    Width="700"
    Height="490"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Height="27"
            ShowMaximize="False"
            ShowMinimize="False">
            <ui:TitleBar.Header>
                <ui:TextBlock Padding="15,0,0,0" VerticalAlignment="Center">
                    Getting Started
                </ui:TextBlock>
            </ui:TitleBar.Header>
        </ui:TitleBar>

        <StackPanel Grid.Row="1" Background="{ui:ThemeResource SystemFillColorNeutralBackgroundBrush}">
            <TextBlock
                Margin="20"
                HorizontalAlignment="Left"
                FontSize="24"
                FontWeight="Bold"
                Text="Welcome to LiveCaptions Translator!"
                TextAlignment="Left" />
            <ui:TextBlock
                Margin="20,10,20,10"
                TextAlignment="Left"
                TextWrapping="Wrap">
                <Run Text="LiveCaptions Translator now uses Whisper Bridge as the only caption input." />
                <Run Text="Before your first run, complete the quick setup below:&#x0A;" />
                <Run Text="&#x0A;1. Start your local ASR bridge service (for example WhisperLiveKit runtime)." />
                <Run Text="&#x0A;2. Confirm the bridge WebSocket endpoint is reachable." />
                <Run Text="&#x0A;3. In the setting page, fill" />
                <Run FontWeight="Bold" Text=" Whisper Bridge URL " />
                <Run Text="and click" />
                <Run FontWeight="Bold" Text=" Apply" />
                <Run Text="." />
                <Run Text="&#x0A;4. When status shows" />
                <Run FontWeight="Bold" Text=" Connected" />
                <Run Text=", switch back to the Caption page to start translation.&#x0A;" />
                <Run Text="&#x0A;Default endpoint: " />
                <Run FontWeight="Bold" Text="ws://127.0.0.1:8765/captions" />
                <Run Text="&#x0A;For more details, visit our wiki: " />
                <Hyperlink NavigateUri="https://github.com/SakiRinn/LiveCaptions-Translator/wiki" RequestNavigate="Hyperlink_RequestNavigate">
                    https://github.com/SakiRinn/LiveCaptions-Translator/wiki
                </Hyperlink>
                <Run FontWeight="Bold" Text="&#x0A;&#x0A;Tip:" />
                <Run Text="Use the Reconnect button in the setting page after restarting bridge services." />
            </ui:TextBlock>

            <ui:Button
                Margin="0,10,0,0"
                Padding="30,10,30,10"
                HorizontalAlignment="Center"
                Click="CloseButton_Click"
                Content="I have configured the bridge." />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
